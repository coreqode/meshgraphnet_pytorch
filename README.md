# Meshgraphnets (Pytorch Implementation)
Pytorch implementation of the Meshgraphnets

- Complete loop is not running right now. 
- Need to simplify the model.

To run the file:
`python -m modules.mgn`
